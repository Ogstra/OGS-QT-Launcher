git submodules init && git submodules update
cd QHotkey
cmake -B build -S . -DQT_DEFAULT_MAJOR_VERSION=6
cmake --build build
sudo cmake --install build

mkdir -p build
cd build
qmake ../OGS-Launcher.pro CONFIG+=debug
make

mkdir -p build
cd build
qmake ../OGS-Launcher.pro
sudo make



debian: qt5-qmake qtbase5-dev libqt5x11extras5-dev cmake
